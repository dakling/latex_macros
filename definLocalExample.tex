\usepackage{xparse}

%load global configuration
\NewDocumentCommand{ \LoadGlobalConfiguration } {  } {
    \IfFileExists{ "$LatexGlobalConfig" }
    { \input{"$LatexGlobalConfig"} } % a global file takes precedence
    { \input{"definGlobal.tex"} } 
}
\LoadGlobalConfiguration

%set local variables
% for example, in presentations, I prefer to use a simpler notation, even if it makes everything longer
\NewDocumentCommand{\EinsteinNotationDiff}{}{\BooleanTrue} % affects second derivatives
\NewDocumentCommand{\ShortHigherMoments}{}{\BooleanTrue} % abbreviate higher one-point velocity moments
\NewDocumentCommand{\ShowArgsTwoPoint}{}{\BooleanFalse} % show arguments eg in H_ij(x,y) 
\NewDocumentCommand{\diff}{m m g}{\diffPartial{#1}{#2}{#3}} % most frequently used type of derivative

% allow align environment to span multiple pages - important for symmetries
\allowdisplaybreaks
